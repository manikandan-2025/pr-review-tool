# Generated outputs — not committed; each user has their own
checkouts/
reports/

# Sensitive credentials — NEVER commit this file
config/secrets.conf

# Per-user settings (ACTIVE_REPO preference etc.) — stays on your machine only
config/settings.local.conf

# Per-user repo registry (local clone paths) — stays on your machine only
config/repos.conf

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
.vscode/
.idea/
